package de.upb.crypto.zeroknowledge.helpers;

public enum Type {
	BOOLEAN,
	GROUP_ELEMENT,
	EXPONENT,
	UNKNOWN
	
	public static Type convert(String type) {
		switch(type) {
			case "boolean": return Type.BOOLEAN
			case "GroupElement": return Type.GROUP_ELEMENT
			case "Exponent": return Type.EXPONENT
		}
		
		
		return Type.BOOLEAN;
	}
}
